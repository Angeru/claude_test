# Secrets for Kamal deployment
# These values are read from environment variables or a password manager
#
# Option 1: Export them in your shell before running kamal:
#   export KAMAL_REGISTRY_PASSWORD=your_docker_hub_token
#   export RAILS_MASTER_KEY=$(cat config/master.key)
#   export SECRET_KEY_BASE=your_secret_key_base
#
# Option 2: Use a .env file (add .env to .gitignore!)
#   Copy .kamal/secrets.sample to .env and fill in values
#
# Option 3: Use 1Password or similar via kamal secrets adapters
#   See: https://kamal-deploy.org/docs/commands/secrets/

KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
RAILS_MASTER_KEY=$RAILS_MASTER_KEY
SECRET_KEY_BASE=$SECRET_KEY_BASE
