<div class="max-w-2xl mx-auto">
  <div class="bg-white shadow-md rounded-lg px-8 pt-6 pb-8 mb-4">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold text-gray-800">Panel de Control</h1>
      <%= button_to "Cerrar Sesión", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-red-500 cursor-pointer" %>
    </div>

    <div class="bg-gray-50 rounded-lg p-6">
      <h2 class="text-lg font-semibold text-gray-700 mb-4">¡Bienvenido, <%= current_user.name %>!</h2>

      <div class="space-y-2">
        <p class="text-gray-600">
          <span class="font-medium">Email:</span> <%= current_user.email %>
        </p>
        <p class="text-gray-600">
          <span class="font-medium">Miembro desde:</span> <%= l(current_user.created_at, format: :long) %>
        </p>
      </div>
    </div>

    <div class="mt-6 p-4 bg-green-50 border border-green-200 rounded-lg">
      <p class="text-green-700 text-center">
        ¡Has iniciado sesión correctamente!
      </p>
    </div>

    <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
      <h3 class="font-semibold text-blue-800 mb-3">Campañas</h3>
      <div class="space-y-2">
        <%= link_to "Ver Campañas Disponibles", campaigns_path, class: "block w-full text-center bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" %>
        <%= link_to "Mis Campañas", my_campaigns_campaigns_path, class: "block w-full text-center bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
      </div>
    </div>

    <div class="mt-6 p-4 bg-teal-50 border border-teal-200 rounded-lg">
      <h3 class="font-semibold text-teal-800 mb-3">Warbands</h3>
      <div class="space-y-2">
        <%= link_to "Mis Warbands", warbands_path, class: "block w-full text-center bg-teal-500 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-teal-500" %>
      </div>
    </div>

    <% if current_user.admin? %>
      <div class="mt-6 p-4 bg-purple-50 border border-purple-200 rounded-lg">
        <h3 class="font-semibold text-purple-800 mb-3">Panel de Administración</h3>
        <div class="space-y-2">
          <%= link_to "Gestión de Usuarios", admin_users_path, class: "block w-full text-center bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-purple-500" %>
          <%= link_to "Gestión de Campañas", admin_campaigns_path, class: "block w-full text-center bg-purple-600 hover:bg-purple-800 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-purple-600" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
