<nav class="relative z-20 px-6 py-4 border-b border-amber-200/60 bg-white/60 backdrop-blur-sm">
  <div class="max-w-7xl mx-auto flex justify-between items-center">
    <div class="flex items-center gap-3">
      <%= link_to root_path, class: "flex items-center gap-3 hover:opacity-80 transition-opacity" do %>
        <div class="w-10 h-10 rounded-full border-2 border-amber-500 flex items-center justify-center bg-amber-100/80">
          <svg viewBox="0 0 24 24" class="w-5 h-5 text-amber-700" fill="currentColor">
            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
          </svg>
        </div>
        <span class="text-amber-700 text-sm tracking-[0.3em] uppercase" style="font-family: 'Cinzel', serif;">SBG Campaigns</span>
      <% end %>
    </div>
    <div class="flex items-center gap-4">
      <% if logged_in? %>
        <span class="text-stone-500 text-sm" style="font-family: 'Crimson Text', serif;"><%= current_user.name %></span>
        <%= link_to "Panel", dashboard_path, class: "text-amber-700 hover:text-amber-600 text-sm tracking-wider uppercase transition-colors", style: "font-family: 'Cinzel', serif; font-size: 0.7rem;" %>
        <%= link_to "Campañas", campaigns_path, class: "text-amber-700 hover:text-amber-600 text-sm tracking-wider uppercase transition-colors", style: "font-family: 'Cinzel', serif; font-size: 0.7rem;" %>
        <%= link_to "Warbands", warbands_path, class: "text-amber-700 hover:text-amber-600 text-sm tracking-wider uppercase transition-colors", style: "font-family: 'Cinzel', serif; font-size: 0.7rem;" %>
        <%= link_to "Skills", skills_path, class: "text-amber-700 hover:text-amber-600 text-sm tracking-wider uppercase transition-colors", style: "font-family: 'Cinzel', serif; font-size: 0.7rem;" %>
        <%= link_to "Equipo", equipments_path, class: "text-amber-700 hover:text-amber-600 text-sm tracking-wider uppercase transition-colors", style: "font-family: 'Cinzel', serif; font-size: 0.7rem;" %>
        <%= button_to "Salir", logout_path, method: :delete, class: "text-stone-500 hover:text-amber-700 text-sm tracking-wider uppercase transition-colors cursor-pointer", style: "font-family: 'Cinzel', serif; font-size: 0.7rem;" %>
      <% end %>
    </div>
  </div>
</nav>
