<!DOCTYPE html>
<html>
  <head>
    <title>Battle Companies Manager - Login / Register</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="auth-bg min-h-screen text-white">
    <% if notice %>
      <div class="fixed top-4 right-4 z-50 p-4 bg-green-700/90 text-white rounded-lg shadow-lg text-sm">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div class="fixed top-4 right-4 z-50 p-4 bg-red-700/90 text-white rounded-lg shadow-lg text-sm">
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
