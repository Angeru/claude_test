<div class="max-w-4xl mx-auto px-4 py-8">
  <%= link_to "← Volver a Miembros", warband_warband_members_path(@warband),
      class: "text-blue-500 hover:text-blue-700 mb-4 inline-block" %>

  <h1 class="text-3xl font-bold mb-6">Editar <%= @member.name %></h1>

  <%= render 'form', warband: @warband, member: @member %>
</div>
