<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <%= link_to "← Volver a rondas", campaign_campaign_rounds_path(@campaign), class: "text-blue-600 hover:text-blue-800" %>
  </div>

  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">Nueva Ronda</h1>
    <p class="text-gray-600 mt-2">Crear una nueva ronda para <%= @campaign.name %></p>
  </div>

  <%= render "form" %>
</div>
