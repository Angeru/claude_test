<div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <%= link_to "← Volver a la ronda", campaign_campaign_round_path(@campaign_round.campaign, @campaign_round), class: "text-blue-600 hover:text-blue-800" %>
  </div>

  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">Editar Resultado</h1>
    <p class="text-gray-600 mt-2">
      <%= @matchup.warband_1.name %> vs <%= @matchup.warband_2.name %>
    </p>
  </div>

  <%= render "form" %>
</div>
